namespace SharpSticks.PlatformInput;

// Placeholder so the SharpSticks.PlatformInput namespace exists in metadata.
// Consumers' global using SharpSticks.PlatformInput; (emitted by the auto-gen
// buildTransitive props) would otherwise fail to resolve, since the project
// itself has no other source files.
internal static class PlatformInputMarker;
