namespace SharpSticks.PlatformDefaults;

// Placeholder so the SharpSticks.PlatformDefaults namespace exists in metadata.
// Consumers' global using SharpSticks.PlatformDefaults; (emitted by the auto-gen
// buildTransitive props) would otherwise fail to resolve.
internal static class PlatformDefaultsMarker;
