namespace SharpSticks.PlatformOutput;

// Placeholder so the SharpSticks.PlatformOutput namespace exists in metadata.
// Consumers' global using SharpSticks.PlatformOutput; (emitted by the auto-gen
// buildTransitive props) would otherwise fail to resolve, since the project
// itself has no other source files.
internal static class PlatformOutputMarker
{
    //public static readonly plat
}
