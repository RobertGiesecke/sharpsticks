namespace SharpSticks.Editor;

// Placeholder so the SharpSticks.Editor namespace exists in metadata. The auto-gen
// buildTransitive props emit a global using for the project's own namespace, which
// would otherwise fail to resolve since the project carries no other source.
internal static class EditorMarker;
